[project]
name = "sable"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TheSecondComing123", email = "78659211+TheSecondComing123@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "typer>=0.27.2",
]

[project.scripts]
sable = "sable:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.9",
]
